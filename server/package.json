{
	"name": "server",
	"version": "1.0.0",
	"description": "The server for easy-notes",
	"main": "index.js",
	"scripts": {
		"lint": "tslint -c tslint.json -p tsconfig.json --fix",
		"tsc:": "tsc",
		"build": "npm-run-all lint tsc",
		"dev:start": "npm-run-all build start",
		"dev": "nodemon --watch src --watch index.ts --exec npm run dev:start",
		"start": "node dist/index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Robin Grun",
	"license": "ISC",
	"dependencies": {
		"express": "^4.17.1",
		"mongodb": "^3.6.9"
	},
	"devDependencies": {
		"@types/express": "^4.17.12",
		"@types/node": "^15.12.1",
		"npm-run-all": "^4.1.5",
		"tslint": "^6.1.3",
		"typescript": "^4.3.2"
	}
}
